# [Project Name]

> **PMM Tier:** P1 — Major Launch
> **Launch Planning Status:** Not Started
> **Target Launch Date:** [date]
> **Marketing Owner:** [assign]

---

## Overview

> [Describe the feature, who it's for, and the business outcome. 2-3 sentences.]

## Key Links

| Resource | Link |
|----------|------|
| PRD | [paste URL → also add to `PRD Link` column] |
| Design Brief / Figma | [paste URL → also add to `Design Brief Link` column] |
| Eng Spec | [paste URL → also add to `Eng Spec Link` column] |
| Research | [paste URL → also add to `Research Link` column] |
| Linear Project | [paste URL → also add to `Linear Link` column] |
| Prototype | [paste URL → also add to `Prototype Link` column] |
| PostHog Dashboard | [link when created] |

## Key Decisions

| Date | Decision | Who | Context |
|------|----------|-----|---------|
| | | | |

## Launch Checklist

### Internal (Tyler owns)

- [ ] Loom video recorded → paste URL in `Loom Video` column
- [ ] SOP generated from Loom AI → create **SOP** page below, paste URL in `SOP` column
- [ ] PRD synced to Notion → paste URL in `PRD Link` column
- [ ] Design brief linked → paste URL in `Design Brief Link` column
- [ ] Eng spec linked → paste URL in `Eng Spec Link` column
- [ ] Research linked → paste URL in `Research Link` column
- [ ] Metrics page created → create **Metrics** page below
- [ ] PostHog instrumentation confirmed (events firing)
- [ ] #product-updates Slack post → paste permalink in `Product Update Slack` column
- [ ] CS team walkthrough completed
- [ ] Update `Internal Docs Complete` multi-select as each doc is linked

### External (Kenzie owns)

- [ ] KB article written → create **KB Article** page below, paste URL in `KB Article` column
- [ ] Storylane interactive demo → paste URL in `Storylane Demo` column
- [ ] In-app onboarding tour (PostHog) → paste URL in `In-App Tour` column
- [ ] Customer email announcement → create **Customer Email** page below, paste URL in column
- [ ] Sales enablement brief → create **Marketing Brief** page below, paste URL in column
- [ ] FAQ document → create **FAQ** page below, paste URL in `FAQ` column
- [ ] Release notes entry
- [ ] Newsletter feature
- [ ] Update `Launch Assets Complete` multi-select as each asset is created

### Launch Status Updates

- [ ] Set `Launch Planning Status` → In Progress (when marketing prep begins)
- [ ] Set `Launch Planning Status` → Ready (when all assets are complete)
- [ ] Set `Launch Planning Status` → Launched (when GA is live)
- [ ] Check `Customer-Facing` checkbox (when first external user gets access)
- [ ] Update `Visibility` at each stage transition

## Weekly Status Log

> Running updates — also synced to `Weekly Status Update` column on the DB row.

**[Date]:** [Status update]

---

## Nested Pages — Auto-Created for P1

The following pages are created inside this project page. As you complete each one, copy its URL and paste it into the corresponding column on the Projects DB row.

> **Instructions for each page:**
> 1. Click the page below to open it
> 2. Fill in the bracketed sections using the template prompts
> 3. When done, copy the page URL (Share → Copy link)
> 4. Go back to the Projects DB table view
> 5. Paste the URL into the matching column

---

### Auto-created pages for this tier:

- KB Article
- SOP
- Marketing Brief
- Customer Email Draft
- FAQ
- PRD
- Design Brief
- Eng Spec
- Research
- Metrics
