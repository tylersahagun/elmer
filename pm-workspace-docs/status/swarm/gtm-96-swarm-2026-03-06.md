# GTM-96 Swarm Kickoff — 2026-03-06

**Issue:** `GTM-96`  
**Lane:** `Lane 0 — Platform Reliability`  
**Primary tracker:** [GTM-96](https://linear.app/askelephant/issue/GTM-96)  
**Related issues:** `GTM-94`, `GTM-95`, `GTM-98`

## Objective
Restore and verify Clerk asset loading on `elmer.studio` so `/login` can be treated as a trustworthy public entry point for the rest of Lane 0.

## Why This Slice Is First
The lane sequencing remains correct:

1. `GTM-96` restore Clerk asset loading
2. `GTM-94` align Clerk / Convex / app-origin configuration
3. `GTM-98` make auth/domain smoke checks trustworthy
4. `GTM-95` finalize deployment/auth runbook
5. `GTM-97` remove stale legacy auth debt

This slice starts first because if Clerk assets do not resolve, later config and smoke-check work cannot be trusted.

## Repo-Confirmed Facts

- The repo assumes a split-domain setup:
  - app origin: `https://elmer.studio`
  - Clerk frontend API host: `clerk.elmer.studio`
  - Clerk account portal host: `accounts.elmer.studio`
- `orchestrator/scripts/check-auth-health.ts` already checks:
  - Clerk publishable key decoding
  - issuer/frontend-host agreement
  - app-origin validity
  - `NEXT_PUBLIC_CONVEX_URL`
  - `/login` HTTP reachability
  - DNS/HTTP for `clerk.*`
  - DNS for `accounts.*`
- `orchestrator/src/lib/auth/clerk.ts` already enforces the same Clerk host / issuer assumptions used by the smoke check.

## Live Evidence Collected

### Local auth-health command
Command:

```bash
npm run check:auth
```

Observed results:

- `PASS Clerk publishable key: frontend API host clerk.elmer.studio`
- `PASS Clerk issuer domain: using https://clerk.elmer.studio`
- `PASS Convex URL: https://fortunate-parakeet-796.convex.cloud`
- `PASS HTTP http://localhost:3000/login: status 200`
- `PASS DNS clerk.elmer.studio`
- `PASS HTTP https://clerk.elmer.studio: status 200`
- `PASS DNS accounts.elmer.studio`

Interpretation:
- the DNS and host-resolution failure described in the original issue is not reproducible from the current local environment
- the Clerk custom-domain path now appears to resolve correctly

### Public login route check
Command:

```bash
curl -I -L --max-redirs 5 https://elmer.studio/login
```

Observed results:

- `HTTP/2 200`
- `x-clerk-auth-status: signed-out`
- `x-middleware-rewrite: /login`

Interpretation:
- the public login route responds successfully
- the route is behaving like a signed-out Clerk entry point

### Browser validation
Observed behavior:

- initial browser snapshot at `https://elmer.studio/login` showed only the shell/notification region and no visible interactive Clerk sign-in UI
- after a short wait, the page settled on `https://elmer.studio/`
- the rendered UI was the Elmer home shell, not a visible Clerk sign-in form

Interpretation:
- network-level and DNS checks are healthy
- browser-visible login behavior is still not correct enough to treat GTM-96 as done
- GTM-96 should remain the active reliability slice until the public `/login` route visibly renders the expected Clerk UI

## Current Hypothesis
The highest-probability interpretation is:

1. the original DNS/custom-domain failure is likely already resolved
2. GTM-96 is now blocked at the browser/UI layer rather than DNS reachability
3. the next likely problem is route/render/auth-flow behavior, which may hand off into GTM-94 once the exact UI failure is understood

## Swarm Workstreams

### Workstream A — Browser validation
Goal:
- confirm the Clerk widget actually renders on the public `/login` page

Evidence needed:
- browser validation of visible sign-in UI
- note whether any asset/network errors remain in the page

### Workstream B — Config consistency handoff
Goal:
- hand off to `GTM-94` only if GTM-96 is truly no longer the blocker

Evidence needed:
- note whether the remaining risk is now issuer/app-origin alignment rather than asset loading

### Workstream C — Runbook sync
Goal:
- ensure the validated state is captured in docs through `GTM-95`

Evidence needed:
- explicit note on whether the custom-domain DNS assumptions in docs match what is now actually live

## Decision Rule

If browser validation confirms that:

- `/login` loads
- Clerk UI renders
- no custom-domain asset errors remain

then GTM-96 should be treated as functionally resolved and the active lane handoff should move to `GTM-94`.

Current result:

- browser validation still does **not** show a working Clerk sign-in experience

Therefore:

- GTM-96 remains the active blocker
- the next step is to capture and debug the route/render/UI failure before attempting the GTM-94 handoff

## Required Linear Update

For this kickoff, Linear should record:

- that a GTM-96 swarm has started
- that current repo and shell evidence shows DNS and basic HTTP are now healthy
- that browser/UI validation is the remaining decisive check
- that the expected next handoff is `GTM-94` if browser validation passes

## Related Docs

- `pm-workspace-docs/status/swarm/elmer-lane0-kickoff-2026-03-06.md`
- `pm-workspace-docs/status/swarm/elmer-swarm-operating-contract.md`
- `pm-workspace-docs/status/swarm/elmer-swarm-dashboard.md`
- `DEPLOYMENT.md`
