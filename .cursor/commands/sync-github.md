# Sync GitHub

Pull GitHub activity into the workspace for visibility.

**Uses**: github-sync

## Usage

- `/sync-github`

## Behavior

- Fetch commits, PRs, and checks as needed.
- Write a sync report to `pm-workspace-docs/status/sync-github.md` using `write_repo_files`.
