# Sync Linear

Pull Linear issues into the workspace for PM review.

**Uses**: linear-sync

## Usage

- `/sync-linear`

## Behavior

- Fetch and normalize Linear issues into signals or tickets.
- Write a sync report to `pm-workspace-docs/status/sync-linear.md` using `write_repo_files`.
