# Sync Notion

Pull Notion inbox items into the workspace.

**Uses**: notion-sync

## Usage

- `/sync-notion`

## Behavior

- Fetch new Notion items and normalize into signals.
- Write a sync report to `pm-workspace-docs/status/sync-notion.md` using `write_repo_files`.
